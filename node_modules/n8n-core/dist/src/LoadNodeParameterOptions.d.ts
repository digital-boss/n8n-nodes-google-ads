import { INode, INodeCredentials, INodeParameters, INodePropertyOptions, INodeTypes, IWorkflowExecuteAdditionalData, Workflow } from 'n8n-workflow';
export declare class LoadNodeParameterOptions {
    path: string;
    workflow: Workflow;
    constructor(nodeTypeName: string, nodeTypes: INodeTypes, path: string, currentNodeParameters: INodeParameters, credentials?: INodeCredentials);
    getWorkflowData(): {
        name: string;
        active: boolean;
        connections: {};
        nodes: INode[];
        createdAt: Date;
        updatedAt: Date;
    };
    getOptions(methodName: string, additionalData: IWorkflowExecuteAdditionalData): Promise<INodePropertyOptions[]>;
}
